<h2><%= @user.name %>さんのマイページ</h2>

<p>
  <strong>名前:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>メールアドレス:</strong>
  <%= @user.email %>
</p>

<% if @user.image? %>
  <%= image_tag @user.image.url %>
<% end %>
<br>
<br>

<% if current_user.id == @user.id %>
  <%= link_to '設定変更', edit_user_path(@user), class: 'btn btn-success' %> |
<% end %>
  <%= link_to '投稿一覧へ', pictures_path, class: 'btn btn-warning' %> 


      